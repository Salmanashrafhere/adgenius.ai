## 2025-05-15 - Global Search Shortcut & Accessibility
**Learning:** Pairing keyboard shortcuts (like `/` for search) with discoverable visual cues (like `<kbd>`) significantly lowers the barrier for power users without cluttering the UI for casual ones.
**Action:** Always include a visual hint and ensure 'Escape' consistently closes the same interaction.
