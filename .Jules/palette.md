## 2025-01-24 - Keyboard Shortcuts and Visual Cues
**Learning:** Pairing global keyboard shortcuts (like `/` for search) with discoverable visual cues (like a `<kbd>` hint) significantly lowers the barrier for power users while keeping the UI clean.
**Action:** Always include a visual hint (e.g., `<kbd>`) when adding keyboard shortcuts to ensure they are discoverable.
